@model IEnumerable<SistemaVenta.Models.DetalleVenta>

@{
    ViewBag.Title = "Index";
}

<h2>Detalle Venta</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Producto.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Venta.Nombre_Cliente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Precio_Venta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sub_Total)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Producto.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Venta.Nombre_Cliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Precio_Venta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cantidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sub_Total)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_DetalleVenta }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_DetalleVenta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_DetalleVenta })
        </td>
    </tr>
}

</table>
