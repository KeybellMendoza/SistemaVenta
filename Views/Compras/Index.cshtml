@model IEnumerable<SistemaVenta.Models.Compra>

@{
    ViewBag.Title = "Index";
}

<h2>Compra</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Total)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaRegistro)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_Provedor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Total)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaRegistro)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Compra }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Compra }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Compra })
        </td>
    </tr>
}

</table>
